name: Keep Render Awake

on:
  schedule:
    - cron: '*/14 * * * *'  
  workflow_dispatch:
    

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping root URL
        run: curl https://proxymapbox.onrender.com/ 
